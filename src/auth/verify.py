"""
Filename: auth/verify.py
Author: ZnPdCo
"""


# Please remove the below line if you need verification of the account credentials.
# pylint: disable=unused-argument
def verify_account(username):
    """
    If you wish to deploy this system,
    please write the `verify_account` function within `src/auth/verify.py` yourself to
    implement user verification for your custom Online Judge (OJ).
    You need to make this function crawl the user profile (or other relevant information) from the
    OJ and check if the profile starts with the prefix `Rating-System-verify`.
    If it does, return `True`; otherwise, return `False`.

    Returns:
        True if the profile starts with the prefix `Rating-System-verify`, False otherwise.
    """
    return True
