{
  "name": "rating-system-backend",
  "version": "0.6.0",
  "scripts": {
    "format": "npx prettier --write app/ && python -m black app/",
    "lint": "pylint app/",
    "dev": "cd app && flask run --debug"
  },
  "author": "ZnPdCo",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-jinja-template": "^2.0.0"
  }
}
